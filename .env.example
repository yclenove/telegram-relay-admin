# 开发：axios baseURL 留空，请求走当前站点 /api/*，由 Vite 代理到 relay（见 vite.config.ts）。
VITE_API_BASE_URL=

# 本地 relay 地址（仅 npm run dev 时生效）；若后端监听其他端口或主机请修改。
VITE_PROXY_TARGET=http://127.0.0.1:8780

# 生产构建（npm run build）时必填，指向线上网关根地址，例如：
# VITE_API_BASE_URL=https://relay.example.com
